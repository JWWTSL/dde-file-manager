elements:
  menu_set_shared_password:
    # Runtime provenance: pyatspi live capture on 2026-09-15.
    # Path: dde-file-manager application -> popup menu DTitlebarMainMenu -> menu item 设置共享密码.
    # Verified by isolated YouQu probe: dtk_main_menu selected this item and opened the shared-password dialog.
    menu:
      - 设置共享密码
  shared_password_message:
    # Runtime provenance: pyatspi after selecting 设置共享密码.
    # Path: dde-file-manager application -> dialog -> label 非匿名访问必须设置共享密码.
    name: 非匿名访问必须设置共享密码
    role: label
  shared_password_cancel_button:
    # Runtime provenance: pyatspi after selecting 设置共享密码.
    # Path: dde-file-manager application -> dialog -> button 取 消.
    name: 取 消
    role: button
  shared_password_confirm_button:
    # Runtime provenance: pyatspi after selecting 设置共享密码.
    # Path: dde-file-manager application -> dialog -> button 确 定.
    name: 确 定
    role: button
